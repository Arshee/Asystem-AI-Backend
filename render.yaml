services:
  - type: web
    name: asystent-ai-backend
    env: node
    plan: starter
    buildCommand: "npm ci && npm run build"
    startCommand: "node dist/index.js"
    envVars:
      - key: PORT
        value: "3000"
